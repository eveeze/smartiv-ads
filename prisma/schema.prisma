// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// --------------------------------------
// ENUMS
// --------------------------------------

enum Role {
  SUPER_ADMIN
  ADVERTISER
  PROPERTY_OPERATOR
}

enum PropertyType {
  HOTEL
  HOSPITAL
  APARTMENT
  VILLA
  RESORT
  MALL
  OFFICE_TOWER
  OTHER
}

enum PropertyClass {
  BUDGET
  STANDARD
  PREMIUM
  LUXURY
}

enum ScreenStatus {
  ONLINE
  OFFLINE
  MAINTENANCE
}

enum ScreenOrientation {
  LANDSCAPE
  PORTRAIT
}

enum AdSlot {
  SCREENSAVER
  INFO_SLIDER
  APP_PROMOTION
  LEISURE_CULINARY
  LEISURE_TOURISM
  LEISURE_GIFT
  WELCOME_GREETING
  BACKGROUND
}

enum RoomCategory {
  STANDARD
  DELUXE
  SUITE
  VIP
  VVIP
  PRESIDENTIAL
  LOBBY
  WAITING_ROOM
  RESTAURANT
  HALLWAY
  ELEVATOR_HALL
  WARD_CLASS_1
  WARD_CLASS_2
  WARD_CLASS_3
  ICU
  ER
  POLYCLINIC
  NURSE_STATION
  LIVING_ROOM
  MASTER_BEDROOM
  KITCHEN_AREA
  PRIVATE_POOL
  OTHER
}

enum CampaignStatus {
  DRAFT
  PENDING_REVIEW
  ACTIVE
  REJECTED
  COMPLETED
  PAUSED
  CANCELLED
}

enum ApprovalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum MediaType {
  IMAGE
  VIDEO
}

enum TransactionType {
  DEPOSIT
  SPEND
  REFUND
  WITHDRAWAL
}

enum WithdrawalStatus {
  PENDING
  APPROVED
  REJECTED
}

enum TransactionStatus {
  PENDING
  SUCCESS
  FAILED
}

// Enum untuk Paket Durasi Campaign
enum DurationPackage {
  DAILY
  WEEKLY
  MONTHLY
  CUSTOM
}

// --------------------------------------
// MODELS
// --------------------------------------

model User {
  id                   Int       @id @default(autoincrement())
  email                String    @unique
  password             String
  name                 String?
  phone                String?
  role                 Role      @default(ADVERTISER)
  isActive             Boolean   @default(true)
  passwordResetToken   String?   @unique
  passwordResetExpires DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  wallet    Wallet?
  campaigns Campaign[]
  logs      AuditLog[]
  media     Media[]

  propertyId Int?
  property   Property? @relation(fields: [propertyId], references: [id])

  @@index([propertyId])
  @@map("users")
}

model Wallet {
  id            Int      @id @default(autoincrement())
  userId        Int      @unique
  user          User     @relation(fields: [userId], references: [id])
  balance       BigInt   @default(0)
  frozenBalance BigInt   @default(0)
  updatedAt     DateTime @updatedAt

  transactions       Transaction[]
  withdrawalRequests WithdrawalRequest[]

  @@map("wallets")
}

model Transaction {
  id            Int               @id @default(autoincrement())
  walletId      Int
  wallet        Wallet            @relation(fields: [walletId], references: [id])
  amount        BigInt
  type          TransactionType
  status        TransactionStatus @default(PENDING)
  referenceCode String?           @unique
  paymentToken  String?
  paymentUrl    String?
  description   String?
  createdAt     DateTime          @default(now())

  @@index([walletId])
  @@map("transactions")
}

model WithdrawalRequest {
  id          Int              @id @default(autoincrement())
  walletId    Int
  wallet      Wallet           @relation(fields: [walletId], references: [id])
  amount      BigInt
  bankName    String
  accountNo   String
  accountName String
  status      WithdrawalStatus @default(PENDING)
  adminNote   String?
  approvedBy  Int?
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt

  @@index([status])
  @@map("withdrawal_requests")
}

model Property {
  id             Int           @id @default(autoincrement())
  name           String
  type           PropertyType  @default(HOTEL)
  classification PropertyClass @default(STANDARD)

  smartivId   Int?    @unique
  smartivCode String? @unique

  baseColor   String?
  activeColor String?
  logoUrl     String?

  address String?
  city    String?

  // Fields untuk Timezone & Region
  timezone String  @default("Asia/Jakarta") // e.g. "Asia/Makassar"
  region   String? // e.g. "Bali", "Jabodetabek"

  enabledSlots AdSlot[] @default([SCREENSAVER])

  // [Phase 13] Revenue Share Percentage (e.g. 0.3 = 30%)
  revenueSharePercentage Float @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  screens        Screen[]
  operators      User[]
  rateCards      RateCard[]
  campaigns      Campaign[]
  blocklist      IndustryCategory[] @relation("PropertyBlocklist")
  publisherLedger PublisherLedger[]

  @@index([classification])
  @@map("properties")
}

model Screen {
  id         Int      @id @default(autoincrement())
  propertyId Int
  property   Property @relation(fields: [propertyId], references: [id])

  name        String
  code        String            @unique
  resolution  String?
  orientation ScreenOrientation @default(LANDSCAPE)
  ipAddress   String?

  roomCategory RoomCategory @default(STANDARD)

  status   ScreenStatus @default(ONLINE)
  lastPing DateTime?

  priceOverride BigInt?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  campaigns   Campaign[]
  impressions ImpressionLog[]

  @@index([propertyId])
  @@index([status])
  @@map("screens")
}

model RateCard {
  id         Int       @id @default(autoincrement())
  propertyId Int?
  property   Property? @relation(fields: [propertyId], references: [id])

  classification PropertyClass?
  targetSlot     AdSlot?

  // Support Paket Harga (Daily, Weekly, Monthly)
  pricePerDay   BigInt
  pricePerWeek  BigInt?
  pricePerMonth BigInt?

  currency String @default("IDR")

  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  @@index([propertyId, targetSlot, isActive])
  @@map("rate_cards")
}

model Campaign {
  id           Int  @id @default(autoincrement())
  advertiserId Int
  advertiser   User @relation(fields: [advertiserId], references: [id])

  propertyId Int?
  property   Property? @relation(fields: [propertyId], references: [id])

  name      String
  startDate DateTime
  endDate   DateTime
  totalCost BigInt
  status    CampaignStatus @default(DRAFT)

  // Fields untuk Logika Baru Campaign
  targetSlot      AdSlot          @default(SCREENSAVER)
  durationPackage DurationPackage @default(CUSTOM)

  rejectionReason String?

  // [Phase 12] Brand Safety Category
  categoryId Int?
  category   IndustryCategory? @relation(fields: [categoryId], references: [id])

  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt
  screens     Screen[]
  items       CampaignItem[]
  impressions ImpressionLog[]

  @@index([advertiserId])
  @@index([propertyId])
  @@index([status])
  @@index([categoryId])
  @@map("campaigns")
}

model CampaignItem {
  id         Int      @id @default(autoincrement())
  campaignId Int
  campaign   Campaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  mediaId Int
  media   Media @relation(fields: [mediaId], references: [id])

  targetRoomCategory RoomCategory[]
  targetSlot         AdSlot

  durationSec Int @default(15)

  // Field untuk interaksi
  actionUrl String?

  // [Phase 11] Optional AdPlacement relation
  placementId Int?
  placement   AdPlacement? @relation(fields: [placementId], references: [id])

  createdAt DateTime @default(now())

  @@index([campaignId])
  @@index([mediaId])
  @@map("campaign_items")
}

model Media {
  id         Int  @id @default(autoincrement())
  uploaderId Int
  uploader   User @relation(fields: [uploaderId], references: [id])

  filename     String
  originalName String
  mimeType     String
  size         Int
  type         MediaType

  // Metadata Media
  title       String?
  displayName String?
  description String? @db.Text
  actionUrl   String? // Default action URL jika tidak di-override di CampaignItem

  url          String
  thumbnailUrl String?
  hlsUrl       String?
  previewUrl   String? // [Phase 10 Step 2] GIF Preview URL
  isTranscoded Boolean @default(false)

  status          ApprovalStatus @default(PENDING)
  rejectionReason String?
  reviewedAt      DateTime?
  reviewedBy      Int?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  campaignItems CampaignItem[]
  tags          MediaTag[]     @relation("MediaTags")

  @@index([uploaderId])
  @@index([status])
  @@map("media")
}

// [Phase 10 Step 4] Tag Model for Media Categorization
model MediaTag {
  id   Int    @id @default(autoincrement())
  name String @unique

  media Media[] @relation("MediaTags")

  @@map("media_tags")
}

// [Phase 11] Ad Placement Specification
model AdPlacement {
  id              Int         @id @default(autoincrement())
  code            String      @unique
  name            String
  width           Int
  height          Int
  aspectRatio     String // e.g. "16:9", "4:1"
  allowedMediaTypes MediaType[]

  createdAt DateTime @default(now())

  campaignItems CampaignItem[]

  @@map("ad_placements")
}

// [Phase 12] Industry Category for Brand Safety
model IndustryCategory {
  id   Int    @id @default(autoincrement())
  name String @unique
  code String @unique

  campaigns  Campaign[]
  properties Property[] @relation("PropertyBlocklist")

  @@map("industry_categories")
}

// [Phase 13] Publisher Revenue Ledger
model PublisherLedger {
  id               Int      @id @default(autoincrement())
  propertyId       Int
  property         Property @relation(fields: [propertyId], references: [id])
  date             DateTime @db.Date
  totalImpressions Int      @default(0)
  totalRevenue     BigInt   @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([propertyId, date])
  @@index([propertyId])
  @@index([date])
  @@map("publisher_ledger")
}

model ImpressionLog {
  id         BigInt   @id @default(autoincrement())
  screenId   Int
  screen     Screen   @relation(fields: [screenId], references: [id])
  campaignId Int
  campaign   Campaign @relation(fields: [campaignId], references: [id])
  timestamp  DateTime @default(now())
  duration   Int

  @@index([screenId])
  @@index([campaignId])
  @@index([timestamp])
  @@map("impression_logs")
}

model AuditLog {
  id        Int      @id @default(autoincrement())
  userId    Int
  user      User     @relation(fields: [userId], references: [id])
  action    String
  details   String?  @db.Text
  createdAt DateTime @default(now())

  @@index([userId])
  @@map("audit_logs")
}